# Assembly Line

Assembly line is a performance maximizeing concerancy library, using an underling multithreading engine that automaticaly handles the thread pool and maximising cpu time.

## TODO

- Add the return data/status to main thread feature.

## IDEAS

- Add a cancel feature that will not run targeted jobs/lines in the queue's.
- Create a async priority list, would also need a job priority switching feature. "Add more queue's"
- Async promise based return instead of active polling.
